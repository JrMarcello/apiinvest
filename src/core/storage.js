import { Storage } from '@google-cloud/storage'

const storage = new Storage()

// const documents = storage.bucket('documents')
// const tedproofs = storage.bucket('tedproofs')

export const uploadFile = (req, res, next) => {
  return 'uri.do.file'
}
